version: '3'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    container_name: chat-frontend
    ports:
      - 5000:3000
#  backend:
#    build:
#      context: ./backend
#      dockerfile: Dockerfile.prod
#    container_name: chat-backend
#    ports:
#      - 3011:3011
